# Auto-generated by stackmux init — customize for your project
# Variables: {{PROJECT}}, {{WORKITEM}}, {{SLOT}}, {{COLOR}}, etc.
# Port variables: WEB_PORT
name: {{PROJECT}}-{{WORKITEM}}

services:

  web:
    container_name: {{PROJECT}}-{{WORKITEM}}-web
    build:
      context: /home/jpasichnyk/projects/personal/stackmux.github.io
      dockerfile: Dockerfile
    ports:
      - "{{WEB_PORT}}:80"
    environment:
      - WORKSTREAM={{WORKITEM}}
      - WORKSTREAM_SLOT={{SLOT}}
    networks:
      - default
      - main_stack

networks:
  default:
    driver: bridge
  main_stack:
    name: {{PROJECT}}_default
    external: true
